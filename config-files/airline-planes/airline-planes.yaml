erver:
  port: 50001
spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  sleuth:
    annotation:
      enabled: true
  data:
    mongodb:
      database: planes
      host: localhost

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-discovery:8761/eureka/